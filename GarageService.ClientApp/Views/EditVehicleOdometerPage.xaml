<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GarageService.ClientApp.Views.EditVehicleOdometerPage"
             xmlns:views="clr-namespace:GarageService.ClientApp.Views"
             >
    <Grid RowDefinitions="Auto,*">
        <views:TitleView Title="Edit Vehicle"
                 SaveCommand="{Binding SaveCommand}"
                 BackCommand="{Binding GoBack}"
                 Grid.Row="0"/>
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="30" Spacing="20">
                <!-- Mileage -->
                <Label Text="Mileage"
                        Style="{StaticResource FieldLabelStyle}"/>
                <Entry Text="{Binding Odometer}"
                       Placeholder="Mileage"
                       Keyboard="Numeric"
                       Style="{StaticResource PrimaryEntry}"/>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>